'use strict';

describe('myApp.challenge module', function() {

  beforeEach(module('myApp.challenge'));

  describe('challenge controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var challengeCtrl = $controller('ChallengeCtrl');
      expect(challengeCtrl).toBeDefined();
    }));

  });
});